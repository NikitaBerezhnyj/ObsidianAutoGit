import { IAutoGitSettings } from "src/types/IAutoGitSettings";

export const DEFAULT_SETTINGS: IAutoGitSettings = {
  token: "",
  language: "en",
  autoCommitEnabled: false,
  autoCommitInterval: 5
};
